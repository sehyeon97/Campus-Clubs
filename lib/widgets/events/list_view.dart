import 'package:flutter/material.dart';

class ViewAsList extends StatelessWidget {
  const ViewAsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Some List Unimplemented atm'));
  }
}
