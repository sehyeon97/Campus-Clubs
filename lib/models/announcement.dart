class Announcement {
  const Announcement({
    required this.title,
    required this.message,
    required this.currentTime,
  });

  final String title;
  final String message;
  final DateTime currentTime;
}
